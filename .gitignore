# --- secrets / local config (NEVER commit) ---
tooling/config.env
.env
server/.env
*.jks
*.keystore
**/key.properties

# --- runtime data (patch store may contain your app binaries) ---
server/_patches/
_patches/
tooling/.state/

# --- build artifacts ---
**/build/
.dart_tool/
.dart_frog/
.packages
pubspec.lock
*.apk
*.aab
*.so
*.dill

# --- engine checkout (huge) + local maven override ---
engine/src/
**/local-engine-maven/

# --- os / editor ---
.DS_Store
*.log
.idea/
.vscode/
